<h1>News Articles</h1><br />
<script>
$(function() {
    $(".ico-delete").click(function(e){
        if (!confirm("Are you sure you want to delete this article?\n\nThis action cannot be undone!")) { e.preventDefault(); }
    });
    $( "#sortable" ).sortable({
        opacity: 0.6,
        update : function () { 
            $('#menuorder').val($('#sortable').sortable('toArray')); 
        } 
    });
    $( "#sortable" ).disableSelection();
    <?php if ($this->deleted) { ?>
        setTimeout(function() {
            $('.done').animate({opacity: "0"}, 1000).slideUp(500);
        }, 3000);    
    <?php } ?>
});
</script>
<?php if ($this->deleted) { ?>
    <p class="msg done">Article deleted successfully!</p>
<?php } ?>
<table>
    <tbody>
        <tr>
            <th>Title</th>
            <th>Date</th>
            <th width="120"></th>
        </tr>
        <?php echo $this->articleList ?>
    </tbody>
</table>
<br />
<a href="/cms-admin/news/add">Add a news article</a>